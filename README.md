# PLAYHAUZ — BingeTown-style Booking (Delhi only)

## Run locally
1) Install Node.js (LTS)
2) In this folder:
   npm install
   npm start
3) Open http://localhost:3000

## Deploy (Render)
- Build: `npm install`
- Start: `npm start`
- Add a Disk and set env:
  SQLITE_PATH=/var/data/playhauz.sqlite
